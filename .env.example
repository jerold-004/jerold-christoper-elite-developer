# Resend configuration for contact form API
RESEND_API_KEY=replace_with_your_resend_api_key
RESEND_FROM_EMAIL=contact@your-domain.com
RESEND_TO_EMAIL=your-email@example.com

# GitHub contribution sync (for exact calendar counts)
# Create a token with read:user scope and keep it server-side only.
GITHUB_TOKEN=replace_with_your_github_token
GITHUB_USERNAME=JEROLD-creator653
